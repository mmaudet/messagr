buildscript {
    ext {
        buildToolsVersion = "37.0.0"
        minSdkVersion = 24
        compileSdkVersion = 37
        targetSdkVersion = 36
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.2.0"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
        // Reads `google-services.json` and turns it into the resources
        // Firebase looks for at runtime. Pinned rather than floating: this
        // one writes generated resources into the build, and a version that
        // moved under a release build would move them with it.
        classpath("com.google.gms:google-services:4.4.2")
    }
}

apply plugin: "com.facebook.react.rootproject"


allprojects {
    repositories {
        maven { url new File(gradle.ext.nodeModules, 'detox/Detox-android') }
    }
}
